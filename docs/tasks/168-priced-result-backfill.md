# Priced result backfill

## Problem

The live MyJam Agency report `4a9a78f9c4a442cea8d19cf21d2d224e` discovered 1,001 primary products but treated the 20-product entitlement as a pre-publication judge cap. Nine of the 20 accepted semantic pairs were later suppressed for missing valid prices, leaving only 11 publishable comparisons.

## Product contract

- A report product limit is a target for publishable, source-linked, same-market comparisons with finite positive supported-currency prices.
- Matching may screen a larger bounded pool to backfill rejected or unpriced candidates.
- Final customer-visible accepted comparisons must not exceed the persisted product limit.
- If the bounded catalog and search pool cannot fill the target, the report must expose the exact shortfall and evidence-backed exhaustion state.
- Price, market, source, and identity safety gates remain unchanged.

## Implementation

- Expand the internal matching pool independently from the customer-visible result target.
- Prefer candidate groups whose primary and rival records already contain valid public prices.
- Increase final enrichment capacity enough to cover backfill candidates.
- Apply the strict price publication gate, then retain the strongest publishable comparisons up to the persisted target.
- Record screened, published, target, and shortfall metrics separately.
- Update the product report metric to describe publishable compared products rather than pre-publication attempts.

## Validation

- Regression tests for candidate pool sizing, priced candidate priority, publication capping, and shortfall metadata.
- Matching route, lifecycle, orchestration, report UI, full test, lint, and build validation.
- Fresh live MyJam paid-plan report proving 20 publishable priced comparisons selected from the catalog-backed screening pool, or an explicit bounded-exhaustion shortfall if the public market truly cannot supply 20.

## Data boundaries

Only attributable first-party public product pages may be published. Search or model output remains discovery/inference and cannot bypass price, source, market, currency, or identity validation.

## Review and validation record

- The installed Claude Code `2.1.238` launcher resolved to Bun `1.4.0` before model selection, so no Claude or Fable review was claimed.
- The required high-risk Codex fallback used two independent, read-only reviewers on the exact PR head.
- The initial fallback reviews blocked hidden target clamping, repeated-rival enrichment that could starve distinct primary products, stale fact-manifest reuse, deleted suppressed evidence, and misleading assessed-versus-published metrics. The revision keeps the purchased target intact, funds at most one completable rival per primary before optional work, reuses facts only when the current manifest ID and hash match, persists the full screened evidence set for evaluation, and labels the customer-visible count as priced products compared.
- The second exact-head fallback review found that screened matches beyond the target could remain price-eligible in the relational API and that a changed retry bundle must not silently reuse a completed stale fact manifest. The revision marks unselected screened rows as evidence-only with `outside-result-target`; exact completed manifests are reusable, while a differing immutable manifest now stops orchestration before replacing authoritative facts or saving a mismatched presentation.
- The next fallback review blocked premature deletion of an authoritative manifest, inaccurate exhaustion wording, and one-rival-only final enrichment. The revision keeps completed facts immutable, distinguishes a fully processed bounded-pool shortfall from incomplete matching/enrichment, and spends remaining price capacity on secondary accepted rivals before presentation images.
- A further exact-head review found a concurrent manifest-finalization race, partial page coverage being treated as complete, and screened count falling back to assessed count. Document persistence now carries an exact expected manifest hash through the worker callback and enforces it with a database CAS; partial page fetches or page-level gaps are limited processing states; and the matcher persists the selected bounded-pool count independently.
- The latest exact-head review found that callers could omit the manifest binding, partial fact artifacts could still be overwritten by an empty binding, and stale positive prices could bypass enrichment before being rejected at publication. The manifest binding is now mandatory end-to-end, an empty binding requires no manifest or chunks, concurrent finalization is guarded atomically, and enrichment applies publication-equivalent source and observation-freshness checks.
- The subsequent exact-head review found that backfill priority still counted stale, cross-market, or cross-currency prices, terminal replay ignored the evidence hash, and retry-time observation drift could change an otherwise identical fact manifest. Backfill priority now requires fresh first-party same-market/same-currency price evidence, terminal replay binds the exact completed manifest, and relational facts use the report's stable observation timestamp across retries.
- The latest exact-head review found recovery ownership, target-market priority, final-enrichment accounting, action-plan determinism, and terminal replay identity gaps. Recovery now atomically adopts an immutable completed fact snapshot for the new attempt; matching priority is bound to the persisted report timestamp and inferred target country; non-product price gaps cannot be counted as scheduled enrichment; action text remains presentation-only and cannot change relational fact identity; and terminal replay validates attempt plus entitlement before returning.
- Two fresh reviews of head `ac549b1` blocked the remaining 80-of-1,000 screening cap, permissive unknown-market publication, retry-time enrichment timestamps, missing recovery ownership for judge checkpoints, attempt-agnostic document/evaluation manifest reads, and replacement of real source observation times with the report timestamp. The revision screens the entire bounded primary catalog, requires affirmative target-market evidence on both sides, plans enrichment against the stable report timestamp, adopts judge checkpoints during recovery, binds document/evaluation reads to the active attempt, and separates real `observedAt` provenance from the stable snapshot timestamp used for retry hashing.
- A final audit found a second hidden cutoff: final price-page enrichment still stopped after 160 pages for a 20-result report. The revision now allows enrichment to exhaust the full 1,000-page bounded pool, so a valid candidate later in the catalog can backfill an earlier unpriced or rejected pair.
- Two exact-head fallback reviews then blocked a two-candidate retrieval ceiling, a 1,000-page enrichment ceiling that could cover only 500 two-sided pairs, retry-time candidate drift, permissive unknown-market publication, and false exhaustion when primaries had no viable candidates. The revision now considers five rivals per primary, persists a compact catalog-bound candidate plan for deterministic retries, counts every synchronized primary as screened, fails publication closed without affirmative target-market proof, and processes bounded enrichment in waves until the 20-result target is filled or all 6,000 planned pages are exhausted.
- The compact 1,000-primary by five-candidate plan is approximately 309 KB, below the 512 KB durable checkpoint limit. Its hash includes product domain, ID, source URL, normalized name, canonical quantity, report timestamp, market, limits, and pins, so changed catalog identity invalidates replay.
- Focused regressions prove that only 20 relational matches remain price-eligible while all screened evidence is retained, differing completed facts fail closed without a terminal document write, processing failures never claim exhaustion, and secondary rivals receive price-enrichment capacity.
- Focused regressions also prove stale positive prices are re-fetched and partial or concurrently finalized fact snapshots cannot be terminalized under an empty binding.
- Prior exact-head validation: all 930 tests passed; production and VPS builds passed with both packaging assertions; lint had zero errors and two pre-existing image warnings; CLI Go tests and vet passed.
- Two fresh reviews of head `e56ceaf` blocked premature collapse of accepted backup candidates, swallowed candidate-plan and judge-checkpoint failures, missing durable enrichment-wave replay, incomplete runtime accounting, false exhaustion for no-candidate primaries, and skipped re-enrichment of publication-ineligible market/currency pairs. The revision retains accepted backups until final priced selection, fails durable plan/checkpoint operations closed, persists each enrichment wave under an exact input hash, budgets checkpoint and fact callbacks, tracks processed primaries independently from assessed candidates, and re-reads both sides of any pair that cannot yet pass the final market/currency publication gate.
- Candidate-plan identity now includes the complete retrieval-affecting product evidence, embedding model and dimensions, canonicalized required URLs and pins, and an explicit plan-algorithm version. Enrichment checkpoint replay validates the exact input hash, bounded coverage, and product shape before reuse; conflict or corruption is a visible incomplete-enrichment state.
- Two independent reviews of `948ac28` blocked retry drift from volatile crawl price/image/timestamp fields, insufficient semantic validation of enrichment checkpoints, and loss of already-committed enrichment after an ambiguous save response. Durable candidate-plan identity now binds stable catalog and retrieval fields while the immutable saved plan itself freezes volatile first-run ranking; replay proves timestamp, image, and price drift cannot replace it. Enrichment checkpoint reuse now requires exact batch counts and matching product/domain/source ownership, validates live results before saving, and reloads a matching committed checkpoint after an uncertain save outcome.
- Two reviews of `c77b064` blocked unexplained target omissions in persisted enrichment, insufficient worst-case judge concurrency, truncated candidate-plan acceptance, and duplicate or wrong-page enrichment outcomes. Every target now requires one source-bound product or explicit source-bound gap; duplicate product outcomes and wrong product paths fail closed. The 1,000-primary route uses concurrency 12, which bounds worst-case embedding plus 200 judge batches below the 720-second application budget, and a versioned candidate plan must prove the full expected selected-primary count and candidate-pair count before replay.
- Two reviews of `fafc228` blocked self-declared candidate-plan completeness, missing persisted checkpoint-integrity verification, product-plus-gap overlap, query-selector market drift, informational missing-robots observations being counted as failed targets, and volatile price/image fields invalidating immutable judge batches. Candidate plans now carry an exact content digest backed by the database checkpoint hash; checkpoint JSON is rehashed on load; enrichment requires exactly one query-bound product or gap per target; missing robots metadata no longer creates a false target failure; and matching judgments exclude volatile price/image availability that belongs to the later publication gate.
- A fresh review of `51b1f3c` found that accepted non-`Product` price gaps could be counted as deferred work while producing no safe page targets, after which absent enrichment metadata could falsely support an exhaustion claim. The zero-target truncated state now persists explicit incomplete enrichment coverage and a limited event, so it can only produce `processing-incomplete`.
- The second review of `51b1f3c` found the same omission for unsafe or missing product source URLs and found that transient match/enrichment incompleteness could terminalize a limited report before Trigger used its durable task retry. Unsafe-source gaps now count as explicit unschedulable work, and any `processing-incomplete` product shortfall throws on a non-final task attempt so the second attempt resumes durable judge and enrichment checkpoints; only the final bounded attempt may save the visible limited state.
- A fresh retry-focused review of `c858b13` found that page-batch checkpoints were durable but their positional enrichment target plan was rebuilt from volatile price/image state. The exact ordered plan now has its own report-attempt checkpoint at index 299, with a stable accepted-product identity hash, exact content digest, semantic validation, and ambiguous-save recovery; retries reuse that frozen plan before replaying page batches at indices 300 and above.
- The next exact-head review found two recovery blockers: retry-varying enrichment-wave telemetry reused an idempotency key, and transient per-page network failures were persisted as permanent batch checkpoints. Enrichment lifecycle events became task-attempt-scoped, and the initial retry correction kept transient gaps out of the immutable completed-batch slot so the next task attempt could re-fetch them.
- Two fresh reviews of `f107120` found that whole-batch retry could discard complementary page successes and that permanent page rejection was incorrectly treated as unfinished processing. Enrichment checkpoints now use immutable task-attempt layers: a retry loads the prior exact batch, fetches only transient gaps, merges complementary successes, and saves the merged layer. Terminal candidate rejections such as verified 404/410 outcomes remain visible but permit bounded exhaustion; network failures, robots limitations, authorization blocks, rate limits, server failures, batch failures, plan limits, and unschedulable targets remain `processing-incomplete`.
- Reviews of `5908c4e` found that a failed transient retry could still discard a prior partial layer, that ambiguous content/HTTP failures needed a fail-closed terminal allowlist, that a later task attempt could discover accepted products conflicting with plan slot 299, and that report recovery could reuse enrichment event identities. Failed refetches now retain the prior verified partial result; body-read failures are retryable; only identity mismatch, unsafe redirect, 404, and 410 are terminal rejection; plan slots are immutable per task attempt and can expand after judge progress; report recovery adopts only judge/candidate checkpoints, not stale enrichment state; and every enrichment event includes report plus task attempt identity.
- Reviews of `0b696a9` found three final orchestration blockers: Salla recovery incorrectly reused the 20-result publication target as its catalog crawl cap, recovered workers reused global progress-event identities, and a final task attempt could save `processing-incomplete` as a misleading terminal limited report. The crawl request now carries a separate 1,000-product catalog screening bound; all nonterminal phase heartbeats are report-attempt and task-attempt scoped; and unresolved processing fails closed on every task attempt. Stable terminal parked/unavailable events remain replay-safe.
- Regressions prove a valid product after item 20 can participate in the final 20 published comparisons, a recovered report and a second task attempt both refresh the database heartbeat, Salla screening remains bounded at 1,000 independently of the result target, and final transient, blocked, unschedulable, corrupt-checkpoint, or batch-failure states never save a customer report. Only complete bounded processing with terminal candidate rejection may publish an honest exhaustion shortfall.
- A review of `6182836` found that the final incomplete attempt still fell through the generic terminal failure handler. Recoverable processing incompleteness now has a distinct error type, skips the terminal `orchestration-failed` event, remains eligible for stale interruption and explicit report recovery, and has a database-backed final-task regression.
- The first review of `bdf53f3` found a hidden pre-matching discovery cutoff: only four primary-product anchors were searched even though downstream matching could claim the bounded pool was exhausted. Discovery now searches 20 distinct catalog anchors, persists the eligible/searched/truncated/complete coverage, and forces any shortfall to remain `processing-incomplete` unless every eligible anchor search completed. Focused tests cover 20-of-25 truncation, a failed product-search lane, and orchestration refusing false exhaustion.
- Two fresh reviews of `2442263` blocked deterministic repetition of the first 20 anchors, silent six-domain candidate truncation, temporary competitor-verification failures being omitted from exhaustion, fallback-lane failures being ignored, and malformed HTTP-200 search output counting as a completed no-result search. Discovery now advances through contiguous 20-product batches only after the prior batch fully completed, repeats incomplete batches, retains up to 20 previously verified rivals, investigates the complete bounded domain set emitted by each wave, and requires schema-valid search output plus complete entity/category/product lanes and terminal rival investigations before exhaustion. Candidate truncation and transient verification failures explicitly keep the report recoverable.
- Two fresh reviews of `0f74a5a` found a remaining seven-catalog matcher ceiling, a verification cap below the actual discovery-wave maximum, loss of remembered rivals across batches, cursor drift when catalog ordering changes, seed-page transient failures being hidden by a successful homepage, and message-only or malformed HTTP-200 provider output being accepted as exhausted search. The revision accepts the full bounded 285-catalog match input or rejects it without slicing, verifies all 132 fresh plus 152 remembered rival domains, retains 152 remembered rivals, binds every cursor to the exact ranked-anchor SHA-256 fingerprint, keeps failed seed-page verification retryable, and requires both a completed web-search call and a structurally complete strict-schema payload. A competitor-memory write failure also prevents cursor advancement.
- Two fresh reviews of `d655bd1` then found that two 20-anchor Trigger attempts could inspect only 40 of 1,000 products, equal-length cursor history could select stale coverage, remembered same-domain evidence could suppress fresh product URLs, only two seed pages and 600 rival products survived downstream, robots/search incompleteness could be mistaken for exhaustion, and the expanded discovery wave exceeded old transport/catalog bounds. The revision runs up to ten bounded attempts, executes product searches in ten-request waves, binds the cursor to the newest exact fingerprinted checkpoint, merges remembered and fresh same-domain evidence, retains up to 5,000 rival products, requires completed provider and tool-call statuses, preserves robots limitations as retryable gaps, and increases the request/task budgets with a two-minute task-ceiling margin.
- Two exact-head reviews of `df2b292` found that later search waves collided with the first immutable matching checkpoint namespace, ten 100-anchor waves left no transient retry capacity, the 152-domain carry-forward limit could support false exhaustion, nested seed-page fetching was unbounded, and successful worker bodies had no transport limit. The revision searches 200 anchors per successful wave so five attempts remain available for transient retries; namespaces candidate-plan and judge checkpoints by task attempt; carries the 500 strongest verified rivals and marks any overflow as non-exhaustive; bounds each competitor to three concurrent page requests and the attempt to 1,712 investigated rivals; accepts up to 1,713 match catalogs; and reads successful worker JSON under an explicit 64 MiB ceiling. A seller may retain all 200 bounded product seeds. The per-query seller policy remains six attributable candidates in the strict provider schema.
- Two exact-head reviews of `2160789` blocked a silent 200-URL same-seller memory cutoff, an apparent 100,000-page historical-memory crawl bound that could exceed the 750-second worker deadline, and unbounded successful discovery-provider bodies. The revision preserves all 6,000 product leads that the declared 1,000-anchor × six-seller policy can produce, enforces that same 6,000-lead limit globally across historical memory with visible truncation, allows the pathological one-seller case, applies a request-scoped 256-page limiter shared across all seller crawls so both skewed and distributed lead sets fit the deadline, and streams provider responses under a 4 MiB limit before parsing.
- Two fresh reviews of `262c1c3` blocked unbounded non-seed path expansion, exact-pair verification replacing accumulated same-seller evidence, historical evidence starving a fresh wave, obsolete recovery checkpoint namespaces, and crawl timing that omitted competitor-verification judge work. Discovered competitors now crawl only the homepage and exact bounded seeds; successful verification unions attributable prior seller evidence; fresh evidence receives the global budget first while historical overflow prevents only final exhaustion, not cursor progress; inferred pairs are judged in bounded batches behind a shared limiter; and the crawl, proxy, stale-worker, and task ceilings are aligned with the 2,400-second declared operation budget.
- The final self-audit found legacy 12-pin caps in both the matcher and its authenticated route could silently omit or reject exact priced leads. Exact backfill now preserves up to the declared 6,000-pair bound end to end, and regression tests prove the thirteenth pin crosses the route, reaches the judge, and remains publishable.
- Two independent reviews of `323df26` found that verified exact pairs were still collapsed to one per seller, the crawl-output and catalog-preservation paths retained 12-pin caps, remembered evidence omitted primary-to-seller bindings, and the 200-batch durable namespace could store only 5,000 of 6,000 pairs. Verification now preserves every accepted bounded exact pair internally, emits a conflict-free 6,000-pin set, persists and validates attributable lead bindings across waves, and reserves 250 judge slots per task attempt (1400–3899) plus candidate-plan slots 3900–3909 with recovery and storage bounds aligned.
- Two fresh reviews of `a0df1ed` found that the authenticated matcher still rejected a 6,000-product single-seller universe at 5,000, report reloads retained only the oldest 100 lifecycle events and could lose later discovery cursors, and indivisible candidate groups could exceed both the 25-pair model request limit and the 250-slot checkpoint namespace. Rival catalogs now accept the full declared 6,000-product bound, report reloads retain 1,000 bounded events, and judge packing splits groups into at most 25-pair chunks while requiring every chunk for a primary to complete before any result for that primary is publishable.
- Current exact-worktree validation: all 979 tests pass, including the new 6,000-product route, split-group, and 151-event cursor regressions; typecheck passes; the production VPS build and both packaging assertions pass; lint has zero errors and the same two pre-existing image warnings; CLI and contract Go tests plus vet pass; and `git diff --check` passes. Two fresh independent exact-head reviews remain required after the final commit.
- Two fresh reviews of `59d7b58` found that rival products were bounded per catalog rather than globally, pins plus ordinary candidates could exceed the 250-slot judge namespace, complete two-sided enrichment may require 7,000 distinct pages, seed-page processing exceptions could falsely advance discovery, a fully searched empty rival pool could never publish truthful exhaustion, and report recovery moved old task namespaces into conflicting new-attempt slots. Rival products and candidate pairs now have separate global 6,000-record bounds; the 7,000-page enrichment universe uses 110 checkpoint batches per task attempt and three concurrent batches while retaining the task-duration margin; seed processing failures remain retryable; an empty rival pool is complete only when orchestration also proves discovery complete; and recovery leaves prior checkpoints immutable under their original report attempt so the new attempt starts with empty namespaces.
- Current exact-worktree validation: all 985 tests pass; typecheck passes; the production VPS build and both packaging assertions pass; lint has zero errors and the same two pre-existing image warnings; CLI and contract Go tests plus vet pass; and `git diff --check` passes. Two new independent exact-head reviews remain required after the next commit.
- Two fresh reviews of `4be8d11` found the matcher library still had a 5,000-product single-seller default and counted exact pins inside the ordinary five-candidate allowance. The library default is now the same 6,000-product bound as the authenticated route, and exact pins supplement the five ordinary retrieval candidates before the separate global 6,000-pair cap is applied, so an unpriced pin cannot displace a viable priced backup.
- Fresh reviews of `6fceb71` found that discovery recovery could consume cursor events from an earlier report attempt and that bounded exhaustion still required a market code when the rival pool was genuinely empty. Cursor reconstruction is now scoped to the active report-attempt idempotency namespace, and only a zero-rival, zero-candidate pool may complete without pair-level market validation.
- Two reviews of `bf25eff` found that a full 6,000-pin universe could silently omit ordinary backups, oldest-first event retention could lose the active recovery cursor, and discovery and matching could select different 1,000-product primary pools. Candidate-plan version 3 durably records pair-universe truncation so it can never support a false exhaustion claim; stored reports retain the newest 1,000 events in chronological order; and crawl now freezes one deterministic discovery-ranked primary catalog before both discovery and matching consume it.
- Current exact-worktree validation: all 990 tests pass; typecheck passes; the production VPS build and both packaging assertions pass; lint has zero errors and the same two pre-existing image warnings; CLI and contract Go tests plus vet pass; and `git diff --check` passes. Two fresh independent exact-head reviews remain required after the next commit.
- Reviews of `6b32746` found that a competitor-memory outage could hide earlier-wave rivals, volatile price/input-order signals could change the bounded primary pool across retries, and a sitemap index with more than four child files could be silently under-read. Memory unavailability is now explicitly non-exhaustive; primary catalog ranking uses only stable identity and catalog evidence; and child-sitemap overflow before 1,000 collected products becomes visible incomplete catalog coverage that blocks exhaustion.
- Reviews of `62a25dc` found that a single child sitemap could itself exceed 1,000 products without exposing truncation, failed root or child sitemap requests could silently support false exhaustion, and the Trigger critical-path proof omitted repeated prior-attempt checkpoint reads plus an enforceable fact-callback ceiling. Sitemap extraction now returns explicit coverage, every failed sitemap fetch is incomplete, orchestration loads the bounded durable checkpoint set once per attempt, and report-fact generation fails closed above the same 250-callback constant used by the runtime budget.
- A fresh exact-head review of `868f5a2` found that the 500-domain competitor-memory window could evict valid priced matches from an earlier discovery wave before a later wave found the remaining products. Every task attempt now stores a tamper-checked, market/attempt/target-bound checkpoint containing only the up-to-20 comparisons that already passed the strict publication gate. Later attempts merge distinct valid primary products from that accumulator, revalidate every source, market, currency, observation timestamp, identity, and positive price, and cannot let an unpriced later observation evict an earlier valid comparison.
- Current exact-worktree validation: all 995 tests pass; typecheck passes; the production VPS build and both packaging assertions pass; lint has zero errors and the same two pre-existing image warnings; CLI and contract Go tests plus vet pass; the enforced worst-case critical path is 13,675,000 ms under the 14,580,000 ms safety boundary; and `git diff --check` passes. Regressions prove ten valid products from one wave plus ten distinct valid products from the next produce one 20-product report, and a later unpriced observation cannot evict an earlier valid priced comparison. Two fresh independent exact-head reviews remain required after the final commit.
- Two fresh reviews of `8f5f889` found that the published-result accumulator was not bound to the exact primary catalog, prior-wave accepted backup evidence could disappear from relational facts, explicit report recovery did not adopt discovery or matching progress, and a legal 1,000-primary/6,000-rival fact bundle could exceed the persistence callback cap. The accumulator now binds the stable discovery fingerprint and sorted primary-catalog identity and rejects foreign primary rows. Complete v2 judge checkpoints freeze their source-bound evidence, recovery copies the immutable discovery/matching namespaces and explicitly records the adopted attempt, orchestration reconstructs prior accepted same-product and close-substitute evidence under the current deterministic vetoes, and fact persistence fails closed rather than saving a terminal dashboard without authoritative facts. The legal callback ceiling is 320 and the worst-case HTTP budget includes the extra checkpoint read while retaining the required task margin.
- New regressions prove that explicit recovery resumes the adopted 200-anchor cursor, stale catalog accumulators are rejected, accepted backup pairs survive in durable screened evidence, judge evidence remains below the 512 KiB checkpoint limit, and fact-persistence failure cannot terminalize a report. A new exact-head commit, full validation, and two fresh independent fallback reviews are required before merge.
- Exact-worktree validation after these corrections: all 998 tests pass; typecheck and Node typecheck pass; production and VPS builds pass with both VPS packaging assertions; lint has zero errors and the same two pre-existing image warnings; CLI and contract Go tests plus vet pass; and `git diff --check` passes.
- Two independent reviews of `4184a8d` blocked immutable v1 judge checkpoints conflicting with the new v2 evidence shape, copied recovery checkpoints colliding when Trigger task numbering restarted, the production-shaped legal fact universe requiring 488 callbacks above the old 320 cap, and a completed fact manifest becoming unrecoverable if the final document callback failed. Recovery now reads the explicit adopted-attempt chain without copying immutable slots; v1 judge checkpoints replay only against their exact hash-bound live groups; a dense 1,000-primary/6,000-rival/6,000-match regression fits below a 512-callback ceiling using a separate two-second local fact-callback budget; and each compact terminal presentation is checkpointed before manifest finalization so a retry can save that exact manifest-bound document without crawling a new wave. Legacy completed manifests repeat their last completed discovery wave instead of advancing evidence.
- Exact-worktree validation after the second correction round: all 999 tests pass; typecheck and Node typecheck pass; production and VPS builds pass with both VPS packaging assertions; lint has zero errors and only the two pre-existing image warnings after test cleanup; CLI and contract Go tests plus vet pass; the worst-case critical path is 12,219,000 ms under the 14,580,000 ms safety boundary; and `git diff --check` passes. Two fresh independent exact-head reviews remain required after the next commit.
- One exact-head reviewer passed `2ab6c45`; the other found that the worst-case duration counted 512 fact-chunk callbacks but omitted the mandatory manifest-finalization callback. The budget now counts all 513 fact callbacks, for a corrected 12,221,000 ms critical path that remains below the 14,580,000 ms safety boundary. Because the reviewed SHA changed, two entirely fresh exact-head reviews remain required.
- Two fresh reviews of `9240ba1` found that report recovery restarted Trigger task numbering and therefore ignored accumulated publication/enrichment layers from later task slots, and that cross-wave publication merging deduplicated only primary products while allowing one rival offering to count more than once. Recovery now scans all ten immutable task-attempt layers (including adopted report attempts), merges every compatible validated publication checkpoint, and writes the new attempt's current accumulator layer. Cross-wave merging now runs a deterministic maximum-cardinality one-to-one assignment over attributable price-eligible rival identities before counting products toward the target. Focused regressions reproduce both failures. A new exact-head commit, full validation, and two fresh independent reviews are required before merge.
- A fresh review of `c46c134` found that the first one-to-one accumulator discarded historical rival alternatives whenever the same primary reappeared in a later wave. The accumulator now unions current and historical price-eligible alternatives for each primary before solving the global maximum-cardinality assignment, so a later shared rival cannot evict an older unique offer and create a false shortfall. The exact overlapping-primary graph is covered by regression. A new exact-head commit, full validation, and two fresh reviews remain required.
