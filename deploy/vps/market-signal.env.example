# Copy to deploy/vps/market-signal.env on the VPS and set chmod 600.
MARKET_SIGNAL_DOMAIN=staging.example.com

# Generate new production values. Never reuse credentials pasted into chat.
MARKET_SIGNAL_CALLBACK_TOKEN=replace-with-at-least-32-random-characters
MARKET_SIGNAL_OWNER_READ_TOKEN=replace-with-a-different-32-character-random-value
MARKET_SIGNAL_OWNER_WRITE_TOKEN=replace-with-another-32-character-random-value
MARKET_SIGNAL_MONITOR_READ_TOKEN=replace-with-a-third-distinct-32-character-random-value
MARKET_SIGNAL_MONITOR_ACK_TOKEN=replace-with-a-fourth-distinct-32-character-random-value
TRIGGER_SECRET_KEY=replace-with-trigger-production-secret
OPENAI_API_KEY=replace-with-a-new-openai-project-key
MARKET_SIGNAL_SEARCH_CHALLENGER_ENABLED=false

# Account identity remains disabled until both values are present. Generate a
# random secret of at least 32 bytes and use the exact public HTTPS origin.
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=https://staging.example.com

# Hosted subscriptions are opt-in. Keep these values server-only and use a
# least-privilege restricted key plus the endpoint-specific signing secret.
MARKET_SIGNAL_HOSTED_BILLING=false
STRIPE_RESTRICTED_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_SOLO=
STRIPE_PRICE_GROWTH=
STRIPE_PRICE_AGENCY=

# Public edge fallback for storefronts that deny the VPS egress IP.

# Temporary read-through migration for reports created before the VPS cutover.
# Remove all three values after the last legacy report has passed its 90-day expiry.
MARKET_SIGNAL_LEGACY_REPORT_RECOVERY_ENABLED=false
MARKET_SIGNAL_LEGACY_REPORT_BASE_URL=https://legacy.example.com
MARKET_SIGNAL_LEGACY_REPORT_SUNSET_AT=2026-12-31T00:00:00.000Z

# Model configuration.
MARKET_SIGNAL_DISCOVERY_MODEL=gpt-5.4-mini
MARKET_SIGNAL_MATCH_MODEL=gpt-5.4-mini
MARKET_SIGNAL_MATCH_EMBEDDING_MODEL=text-embedding-3-small
MARKET_SIGNAL_AD_MODEL=gpt-5.4-mini
MARKET_SIGNAL_MODEL=gpt-5.6-luna
MARKET_SIGNAL_DEFAULT_PLAN=starter
MARKET_SIGNAL_PLAN_REGISTRY_JSON={"myjam.co.uk":"agency"}

# Optional providers. Leave blank to expose an honest unavailable state.
METAPI_API_KEY=
META_AD_LIBRARY_ACCESS_TOKEN=
